const nome = 'kaike' //string
const nome1 = "kaike" //string
const nome2 = `kaike` //strinf

const num1 = 10 // number
const num2 = 10.53 // number
let nomeAluno; // undefined = não aponta para local nenhum na memoria
const sobrenomeAluno = null // Nuli > Não aponta para local nenhum na memoria
const aprovado = true; // Boolean = true, false (logico)
